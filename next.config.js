/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "images.unsplash.com",
      "loremflickr.com",
      "picsum.photos",
    ],
  },
}

module.exports = nextConfig
